export enum StatusPedido {
    PENDENTE = "pendente",
    PREPARANDO = "preparando",
    ENTREGUE = "entregue",
    CANCELADO = "cancelado"
}

export enum StatusPagamento {
    PENDENTE = "pendente",
    PAGO = "pago",
    RECUSADO = "recusado",
}